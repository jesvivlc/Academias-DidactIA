# DidactIA — Registro de cambios

> Referenciado desde CLAUDE-ROADMAP.md. Ver también @CLAUDE-MODULOS.md.

## Registro de cambios recientes

- `2026-06-19` — feat(agente): **agente de comunicación a familias (modo asistido)** (`js/comunicados.js`). Tercer agente. Botón "🤖 Agente: redactar" en Comunicados → `agenteComunicacion()`: a partir de una orden breve, Gemini (`_comIA`, role:familia) redacta el comunicado (título + cuerpo cordial/profesional) e **infiere los destinatarios** (todas las familias / `grupo:XXXX` / todo el centro / profesorado) → JSON parseado (`_agcParse`). Borrador editable (asunto, mensaje, destinatarios + grupo) → "✓ Revisar y enviar" → núcleo `_comEnviarNucleo` (insert en `comunicados` + email `send-comunicado` + push `_comPushFamilias`). Cada familia lo recibe en su idioma al abrirlo (multi-idioma `_comTraducir` ya existente). Modo asistido. SW v16→v17.
- `2026-06-19` — feat(agente): **agente de RRHH/permisos (modo asistido)** (`js/rrhh.js`). Segundo agente. Botón "🤖 Agente: evaluar pendientes" en RRHH → `agenteRRHH()`: evalúa TODAS las solicitudes pendientes contra la normativa del centro (concertada/privada/EBEP, según `_rrhhGetRegimen`) en secuencia con barra de progreso (`_rrhhEvaluarUna` → `_rrhhGeminiLegal`), y monta una bandeja con, por solicitud: recomendación (aprobar/denegar/revisar con color), explicación + fundamento orientativo, alerta, y un mensaje al profesor ya redactado y editable; botones "Aprobar/Rechazar y enviar" por solicitud + "Aprobar las N recomendadas" (bulk de las claras). Refactor: `_rrhhResolver` extrae `_rrhhAplicar(id,accion,mensaje)` (núcleo sin UI: estado + nota_resolucion + sustituciones + push al profesor), reutilizado por el modal individual y por el agente. SW v15→v16.
- `2026-06-19` — feat(agente): **agente de sustituciones autónomo (modo asistido)** (`js/agente.js` nuevo + refactor `js/admin.js`). Primer agente de DidactIA. Botón "🤖 Agente: cubrir el día" en Sustituciones → `agenteSustituciones(fecha)`: expande ausencias de día completo a tramos y produce un plan de cobertura con **asignación global**: prioriza profesorado **de guardia** por tramo, luego libres por **equidad**; **nunca asigna el mismo profesor a dos clases a la vez** (`usedByHora`); reparte la carga del día (`load` proyectado) y resuelve antes los tramos con menos candidatos (escasez primero). **Briefing** en lenguaje natural + tabla con cada propuesta como desplegable editable (guardia 🛡/libre con su carga) + avisos de tramos sin cobertura. **Modo asistido**: propone, tú confirmas → "Confirmar y asignar" ejecuta todo (UPDATE + guardia + email a ambos vía notify-sustitucion + push al sustituto). Refactor de `_sustCalcularLibres` para exponer `guardia` y `libres` por separado (compat conservada para el formulario y "+ Asignar"). El botón "Plan manual" (`planCoberturaDia`) se conserva como alternativa. SW v14→v15.
- `2026-06-18` — feat(rrhh): **copiloto legal IA para permisos** (`js/rrhh.js`). Botón "✨ Evaluar (IA)" en cada solicitud pendiente + selector de **régimen jurídico** del centro (concertada / privada / funcionario público), recordado por centro en localStorage (`rrhh_regimen_{ctrId}`). `_rrhhEvaluarIA` → modal con los datos de la solicitud → `_rrhhLanzarEval` consulta a Gemini (EF `chat`, `role:'familia'` para forzar texto) la normativa aplicable (convenio de enseñanza concertada / privada / EBEP y función pública docente) y devuelve JSON: `recomendacion` (aprobar/denegar/revisar, con color), `permiso_tipo` (retribuido/no/deber inexcusable/IT), `fundamento` orientativo, `requisitos[]`, `dias`, `explicacion`, `alerta`. Botones Aprobar/Rechazar en el resultado (reutilizan `aprobarAusencia`/`rechazarAusencia`). **No vinculante** (disclaimer visible; el prompt recomienda "revisar" ante ambigüedad y no inventa artículos exactos). Importación de las **212 guardias de Buñol** desde el PDF de horarios el mismo día (ver `scripts/importar_guardias_pdf.py`). SW v12→v13.
- `2026-06-18` — data(buñol): **importadas 212 guardias de 99 profesores** a `horarios_grupo` (actividad `Guàrdia`, grupo centinela `GUÀRDIA`) desde el "Horari setmanal del docent" en PDF, vía `scripts/importar_guardias_pdf.py`. Sin esto el Plan de cobertura no podía sugerir al profesor de guardia de cada tramo (el horario importado solo traía las clases). Verificado: lunes 11:15 → 5 profesores de guardia.
- `2026-06-18` — feat(sustituciones): **Plan de cobertura del día + matcher de nombres robusto** (`js/plancobertura.js` nuevo + `js/admin.js` + `js/rrhh.js`). Segunda iteración tras feedback de Buñol. (1) **Matcher robusto por tokens** `_sustNombreCoincide`: el nombre del perfil (`Bruno Sánchez`) ahora encuentra el del horario (`SÁNCHEZ GONZÁLEZ BRUNO` — orden invertido + tildes + comas) verificando que todos los tokens significativos del suelto estén en el del horario. Verificado contra datos reales: matchea las 2 variantes de Bruno y no confunde con otros Sánchez. (2) **`_crearSustituciones`** (rrhh.js) usa el matcher en vez de `ilike '%nombre%'` (que no encontraba nada) → genera filas **por tramo** con las horas reales del centro. (3) **`_sustCalcularLibres`** excluye ahora también a los profesores **ausentes** ese día/tramo (no solo a los que tienen clase). (4) **Plan de cobertura del día** (`window.planCoberturaDia`): modal con cada tramo sin cubrir del día → sustituto **sugerido** (libre con menos guardias) preseleccionado + dropdown editable (toggle "todos") + "Asignar" por tramo + "Asignar todas las sugeridas"; expande filas de día completo (`tramo=null`) en filas por tramo según el horario real del ausente; al asignar: UPDATE + guardia + email a ambos + push. Botones de Sustituciones repuntados al Plan (el agente conversacional `buscarProfesorLibreAgente` queda sin uso). (5) **SW v11→v12** + `fetch(cache:'no-cache')` para no servir JS viejo (causa de que los duplicados parecieran persistir pese a que el dedup ya reducía 137→88 nombres en Buñol).
- `2026-06-18` — fix(sustituciones): **flujo RRHH→Sustituciones reparado (reportado en IES Buñol)** (`js/admin.js` + EFs `notify-ausencia`/`notify-sustitucion`). (1) **Profesores duplicados** en el selector: Buñol tiene cada profesor con dos variantes de nombre (`APELLIDOS NOMBRE` y `APELLIDOS, NOMBRE`); el `Set` exacto no las unía. Nuevos `_sustNombreNorm`/`_sustDedupeNombres` (mayúsculas + sin tildes + sin comas + espacios colapsados) deduplican y, lo importante, normalizan también la detección de ocupados/guardia (un profe ocupado bajo una variante ya no aparece libre). (2) **No se podía asignar el sustituto a la ausencia**: la fila pendiente solo permitía "marcar cubierta" (sin sustituto) o borrar; asignar obligaba a usar el formulario, que creaba una fila NUEVA → duplicado. Nuevo botón **"+ Asignar"** en cada fila pendiente → modal con profesores libres por equidad (toggle "mostrar todos") → `UPDATE profesor_sustituto+cubierta` sobre la MISMA fila (`window._sustAsignar`/`_sustConfirmarAsignar`). (3) **Aviso a ambos**: al asignar se llama a `notify-sustitucion` (email a ausente y sustituto) + push al sustituto + `registrarGuardiaEnBD` (equidad). (4) **Emails con hipervínculo**: `notify-ausencia` (a jefatura) y `notify-sustitucion` ahora llevan botón "Abrir/Gestionar en DidactIA →" a `https://didactia.eu/app.html`. Núcleo reutilizable `_sustCalcularLibres(fecha,hora)` compartido por el selector del formulario y el modal. Buñol: 0 actividades 'guardia' → el modal muestra todos los libres (no en clase ese tramo). EFs redesplegadas.
- `2026-06-17` — fix(seguridad RGPD): **auditoría RLS de las tablas de la sesión + fuga de Storage corregida**. Auditadas vía `pg_policies` las 10 tablas nuevas (eventos_centro, encuestas, encuesta_respuestas, menu_comedor, recursos, prestamos, actas_reunion, documentos_centro, tutoria_espera, comunicado_lecturas): políticas correctas (staff-only donde toca; familia solo `visible_para`/borradores/propios). **Fuga corregida**: el bucket privado `documentos-centro` tenía `docc_read` permitiendo a cualquier miembro del centro (incl. familia) descargar **cualquier** archivo por ruta, saltándose `visible_para` (la RLS de tabla solo oculta la ruta, no el objeto). Fix: la ruta ahora es `{centro}/{visible_para}/{archivo}` y `docc_read` exige que una familia solo lea bajo `todos/`/`familias/` (`js/documentos.js` actualizado; 0 objetos previos → sin migración). Test `tests/rls-familia.spec.js` ampliado (paso 11): recursos/prestamos/actas_reunion → 0 filas; encuestas sin borradores; documentos/eventos sin `staff`; INSERT bloqueado en recursos/actas/eventos. Nota aceptada (no es fuga): encuestas anónimas (`familia_id=NULL`) sin UNIQUE → inherente al anonimato.
- `2026-06-17` — feat(comunicados): **registro central de lectura** (`js/comunicados.js` + tabla `comunicado_lecturas`). `_comMarkLeido(id)` ahora, además del `localStorage` por dispositivo, hace `upsert` idempotente (UNIQUE comunicado_id+user_id, `ignoreDuplicates`) en `comunicado_lecturas` al abrir un comunicado. El dashboard de participación (`js/participacion.js`) añade KPI "Leyeron comunicados (%)" y una sección "Lectura de comunicados" (familias que abrieron / destinatarias, por comunicado). RLS: cada usuario rw lo suyo; staff lee todo. Métrica prospectiva (cuenta desde ahora).
- `2026-06-17` — feat(analisis): **dashboard de participación familiar** (`js/participacion.js`, 3ª pill en Análisis). KPIs: familias registradas, con push activo (%), que respondieron alguna encuesta (%), que solicitaron tutoría (%); tasa de autorización de salidas; tabla de respuesta por encuesta (familias respondientes / destinatarias, con denominador por grupo vía `familia_alumno` para encuestas dirigidas). Solo lee tablas existentes; nota de que la lectura de comunicados no se contabiliza (es por dispositivo, en localStorage). `analisisTab()` en `app.html` ampliado a 3 estados.
- `2026-06-17` — feat(export): **exportación contable** (`js/comedor.js` + `js/recursos.js`). Comedor: botón "Informe mensual de facturación" (selector `type=month`) → `comedorInformeMensual()`: Excel con comidas por alumno del mes distinguiendo menú (facturable) de tupper, totales, y si se indica precio/menú → importe por alumno y total. Recursos: botón "Exportar Excel" en Préstamos → `_recExportar()`: todos los préstamos con estado (activo/vencido/devuelto), fechas y notas. Ambos con SheetJS (ya cargado).
- `2026-06-17` — feat(tutorias): **lista de espera** (`js/tutoria.js` + tabla `tutoria_espera`). Si todas las franjas de un día están ocupadas, la familia ve "🔔 Apuntarme a la lista de espera" (`_tutFamApuntarEspera`). Al cancelarse una cita (tutor o familia), `_tutAvisarEspera(tutor, fecha)` push a la 1ª familia en cola ("hueco disponible") y marca `notificado`. Al reservar, la familia sale de la lista de espera de ese día. RLS: staff ALL + familia propio.
- `2026-06-17` — feat(documentos): **biblioteca de documentos del centro** (`js/documentos.js` + tabla `documentos_centro` + bucket `documentos-centro`). Nav "📁 Documentos" (todos los roles; RLS filtra por `visible_para`). Dirección sube archivo (bucket privado, signed URL 1h) o enlace externo, con categoría (circular/normativa/PGA/calendario/formulario/otro) y visibilidad (todos/familias/staff); resto descarga según rol. Filtro por categoría con conteo, tarjetas con icono por tipo. Bucket + 4 políticas de Storage por centro. SW v10→v11.
- `2026-06-17` — feat(sustituciones): **previsión de cobertura** (`js/prevision.js`, sin tabla nueva). Botón "Previsión" en la vista admin de Sustituciones → `window.preverCobertura(dias=14)`: query de sustituciones futuras sin cubrir (próximos 14 días, `cubierta=false/null` y `profesor_sustituto` nulo), cruza `horarios_grupo` (día normalizado sin tilde + tramo) para detectar profesores libres en cada slot, ordena por equidad (`getGuardiaCountsByName`, menos guardias del trimestre primero). Modal agrupado por fecha con selector top-5 y "Asignar" en un clic (`_prevAsignar` → UPDATE `sustituciones` profesor_sustituto+cubierta, `registrarGuardiaEnBD`, `notify-sustitucion`). Reutiliza helpers de `guardias.js`/`admin.js`.
- `2026-06-17` — feat(actas): **resumen IA de claustros** (`js/actas.js` + tabla `actas_reunion`). Nav "📝 Actas" (grupo Administración, dirección/admin/jefatura). Dictado por voz (Web Speech API es-ES) o pegado de notas → "✨ Generar acta con IA" (EF `chat` con `role:'familia'` para forzar texto, JSON: resumen/acuerdos/tareas con responsable/plazo, editable). Lista por fecha, vista de detalle, exportación PDF (jsPDF, reutiliza `_infEnsureLibs`/`_infCentroInfo`/`_infHexToRgb` de `informes.js`). RLS `actas_all` de gestión por dirección.
- `2026-06-17` — feat(recursos): **préstamo de recursos** (`js/recursos.js` + tablas `recursos` + `prestamos`). Nav "📦 Recursos" (personal del centro, no familia). Dos pills: Préstamos (activos con resaltado de vencidos + historial, "Devolver" en un clic) e Inventario (alta/categoría/código/notas, dar de baja/reactivar/eliminar, "Prestar"). Al prestar/devolver se sincroniza `recursos.estado` (disponible↔prestado). RLS staff-only por centro.
- `2026-06-17` — feat(comedor): **menú semanal del comedor** (`js/menu.js` + tabla `menu_comedor`). Nueva vista "📋 Menú" en el panel de Comedor (3ª pestaña, `showComedorVista('menu')` ampliado): dirección edita el menú diario (1º/2º/postre/notas) por semana con navegación semanal + upsert por `(centro,fecha)`; familias y personal lo ven en formato semanal de solo lectura. El menú de la semana actual también aparece en la home de familias (`renderMenuComedor("fh-menu",{hideIfEmpty:true})` en `renderHomeFamilia`). RLS lectura todo el centro / gestión dirección.
- `2026-06-17` — feat(encuestas): **encuestas a familias** (`js/encuestas.js` + tablas `encuestas` + `encuesta_respuestas`). Nav "📊 Encuestas". Dirección crea encuestas con preguntas (escala 1–5, opción múltiple, sí/no, texto), destinatarios (todas las familias o grupo), anónima opcional y fecha de cierre; estados borrador→abierta→cerrada; publica con push a familias destinatarias; resultados agregados (medias, distribución en barras de %, textos). Familia responde desde su panel (anti-doble-respuesta vía UNIQUE; anónima → `familia_id` null). RLS por centro/rol.
- `2026-06-17` — feat(agenda): **eventos del centro** (`js/agenda.js` + tabla `eventos_centro`). Botón "+ Nuevo evento" (admin/dir/jefatura) en la Agenda → modal (título, fecha, hora opcional, tipo claustro/reunión/festivo/evaluación/plazo, visible_para todos|staff, descripción) → INSERT. Se pintan como 5ª fuente en el calendario (color teal `#1F7A8C`, leyenda + icono 🗓️), borrables desde el panel de día por staff, y se exportan al `.ics`. Familias solo ven `visible_para='todos'` (RLS `ev_read`/`ev_manage`). SW v9→v10 (+ precache de tutoria/agenda/encuestas/menu/recursos/actas/prevision).
- `2026-06-17` · `a3b3c84` — fix(tramos): **auditoría y eliminación de 9 tiempos hardcodeados de Agora en 5 archivos** (`js/admin.js` + `app.html` + `js/chat.js` + `js/espacios.js` + `js/planner.js`). Raíz del problema: todos los mapas de horas (tramoData, tramoLabels, tramoHoras, detectarTramoActual) usaban los tramos de Agora Lledó (08:50, 09:45…) en lugar de leer `tramos_centro` del centro activo, impidiendo a IES Buñol (y cualquier otro centro) registrar sustituciones o reservar espacios correctamente.
  - **`js/admin.js`**: (1) `_getTramosData()` — nuevo helper async con caché por `ctrId` (auto-invalida al cambiar de centro); (2) `detectarTramoActual()` → ahora async, compara hora actual contra `tramos_centro` reales; (3) `initSustPanel()` → repuebla `#sust-tramo` con las opciones del centro antes de marcar el tramo actual; (4) `registrarSustitucion()` → usa `await _getTramosData()` en lugar del `tramoData` local de Agora; (5) `cargarProfesoresLibresEnSelect()` → ídem; (6) fallback en `_loadTramosNotif` y `notificarAusenciaProfesor` → tiempos genéricos en horas redondas (08:00, 09:00…) en lugar de los de Agora.
  - **`app.html`**: `<select id="sust-tramo">` ya no tiene las 8 `<option>` estáticas de Agora; queda con un solo placeholder y `initSustPanel()` lo rellena con los tramos del centro.
  - **`js/chat.js`**: `tramoHoras` ordinal ("primera"→"08:50"…) eliminado; sustituido por `_chatTramosOrd` — caché a nivel de módulo rellenada en `buildContext()` consultando `tramos_centro` y mapeando por índice de posición (tramo 1=primera, tramo 2=segunda…); si el caché no está listo aún usa horas redondas genéricas.
  - **`js/espacios.js`**: `_espGetTramos()` — nuevo helper async con caché por `ctrId`; `loadEspacios()` construye `tramoLabels` y la lista de columnas dinámicamente; `reservarEspacio()` lee `hora_inicio`/`hora_fin` del mismo caché.
  - **`js/planner.js`**: log cosmético `"centro destino (Agora)"` → `"centro destino"`.

- `2026-06-16` — feat(agenda): **Agenda del Centro — calendario mensual unificado** (`js/agenda.js` nuevo + `app.html` + `js/auth.js`). Vista agregada de 4 fuentes de datos sin tabla propia. **Split-view**: grilla mensual CSS Grid 7 cols (izquierda 340px) + panel del día (derecha flex:1). Navegación ± meses con `window._agNavMonth(delta)`. Dots de color en cada día: danger=sustituciones sin cubrir, success=cubiertas, `#7A5C9E` (custom `ag-c-tut`)=tutorías, info=salidas, warning=ausencias aprobadas. `_agFetchEvents(from, to)` — 4 queries en `Promise.all`: `sustituciones`, `tutoria_citas`, `salidas_didacticas`, `ausencias_profesor` (omitida para familias); RLS filtra automáticamente lo apropiado por rol. Ausencias multi-día expandidas a días individuales en cliente. Al clicar tarjeta de evento → navega al módulo origen (`showTab('sust'/'tutorias'/'salidas'/'rrhh')`). Visible para todos los roles autenticados. CSS self-contained `_agEnsureStyles()` (idempotente). Responsive ≤768px: columnas apiladas + `scrollIntoView` al panel del día. Cableado: `nav-agenda` grupo Centro (color `#5B7FA8`, icono `ti-calendar`), `panel-agenda`, TAB_MAP `tab-agenda`, TITLES, MAS_CFG; `showTab('agenda') → initAgenda()` en `patchShowTab` (app.html) y auth.js.

- `2026-06-16` · `37865ab` — feat(tutorias): **módulo de reserva de citas tutor-familia** (`js/tutoria.js` nuevo + `supabase/migrations/tutorias.sql` + `app.html` + `js/auth.js`). Dos tablas: `tutoria_disponibilidad` (ventanas semanales del tutor: grupo_horario, dia_semana 1–5, hora_inicio/fin, duracion_min) + `tutoria_citas` (UNIQUE(disp_id, fecha, hora_inicio) para evitar doble reserva; alumno_nombre/grupo_horario denormalizados). RLS: staff ALL por centro; familia SELECT activas en disponibilidad; familia SELECT/INSERT/UPDATE propias en citas. **Vista profesional**: tabs "Mis citas" (confirmar/cancelar/realizada, notas_tutor guardadas onblur) + "Mi disponibilidad" (CRUD formulario: grupo auto-detectado de horarios_grupo, día, hora, duración). **Vista familia**: selector de hijo (chips) → disponibilidad del tutor del grupo del hijo → date picker → sub-slots generados en cliente (ventana / duracion_min) → tomados marcados con query a BD → solicitar; tab "Mis citas" con historial y cancelación. **Vista admin/dirección**: tabla read-only de todas las citas. Push tutor←→familia vía EF `send-push` (fire-and-forget): nueva solicitud → tutor; confirmar/cancelar → familia; familia cancela → tutor. Error 23505 (doble reserva) → toast explicativo. CSS self-contained `_tutEnsureStyles` (idempotente). Cableado: `nav-tutorias` grupo Docencia, `panel-tutorias`, `tab-tutorias`, TAB_MAP, TITLES, MAS_CFG; visible para profesional/familia/admin/director/jefatura/admin_institucional/superadmin; `showTab('tutorias') → initTutorias()`.
- `2026-06-16` · `99f3006` — feat(familia): **push a familias al enviar un comunicado** (`js/comunicados.js`). `_comPushFamilias(comunicadoId, destinatarios, titulo)` — fire-and-forget tras `send-comunicado`: `'grupo:XXXX'` → familias de alumnos de ese grupo; `'todos'`/`'solo_familias'` → todas las familias del centro (`profiles rol=familia`); `'solo_profesores'` → sin push. Mensaje "📢 Nuevo comunicado" + el título. Completa el suite de avisos push a familias: comedor, asistencia, salidas, trámites de orientación, notas, incidencias y comunicados.
- `2026-06-16` · `442c2fa` — feat(calificaciones): **boletín PDF también en la vista dirección** (`js/calificaciones.js`). Refactor `_calBoletinPDF(hijo?, btnId?)` — sin args usa alumno seleccionado (familia); con args usa `window._calAdminAlumno {nombre,grupo}` guardado por `_calAbrirAlumno`. Botón "⬇ Boletín PDF" en la cabecera del split detail de dirección. Mismo PDF para ambos roles.
- `2026-06-16` · `fe8c566` — feat(familia): **boletín de calificaciones en PDF** (`js/calificaciones.js`). `_calBoletinPDF()` — botón "⬇ Boletín PDF" en vista familia. Cabecera logo+color del centro (reutiliza `_infEnsureLibs`/`_infCentroInfo`/`_infImgToDataURL`/`_infHexToRgb` de `informes.js`) + tabla pivote asignatura × evaluación (1ª/2ª/3ª/Final) + profesor. jsPDF + autotable on-demand.
- `2026-06-16` · `fe7eba4` — feat(familia): **push de nuevas notas e incidencias a las familias** (`js/calificaciones.js` + `js/incidencias.js`). Calificaciones (`calGuardar`): `_calNotificarFamilias` — al guardar, diff `data-nota-orig` vs valor actual; solo avisa si cambió, solo una vez (actualiza el orig tras guardar). Incidencias (`notificarFamiliaIncidencia`): `_incPushFamilia` — además del email, push genérico "⚠️ Comunicación del centro" (mensaje sin detalles, respeta la decisión manual del admin). Ambos fire-and-forget vía EF `send-push`.
- `2026-06-16` · `637acee` — feat(pwa): **app instalable para familias + SW v8** (`manifest.json` + `sw.js` + `app.html` + `js/familias.js` + `js/auth.js`). `manifest.json`: `start_url: /app.html`, `scope`, `id`, `categories`, `lang`, icono maskable. SW v8: precache completo (añadidos `alumnos.js`/`asistencia.js`); icono de notificación inline (no depende de `/icon-192.png`); `notificationclick` enfoca ventana abierta o abre `/app.html`. `app.html`: `apple-touch-icon` + captura `beforeinstallprompt` → `window._deferredInstallPrompt`. `initInstallBanner` (familias.js, llamada desde auth.js tras login): banner "📲 Instalar app" — prompt nativo Android/Chrome; instrucción Compartir→Añadir en iOS; descartable (localStorage); no muestra si `display-mode:standalone`.
- `2026-06-16` · `b53abf6` — fix(storage): **bucket 'documentos' faltante** (`supabase/migrations/bucket_documentos_justificantes.sql` + `js/admin.js`). `js/admin.js` subía justificantes a `sb.storage.from("documentos")` pero ese bucket nunca existió (solo `documents` y `materiales`) — el upload tenía `.catch(()=>{})` → los justificantes se perdían en silencio. Migración (✅ aplicada en prod): bucket PRIVADO `documentos` + 4 políticas Storage staff-only por centro (path `justificantes/{centro_id}/…`). `admin.js`: el catch silencioso ahora hace `console.warn`. `CLAUDE-TABLAS.md` actualizado.
- `2026-06-16` · `eab94f3` — chore(qa): **verify-columns + CI static-checks** (`scripts/verify-columns.js` + `scripts/schema-snapshot.json` + `.github/workflows/weekly-check.yml` + `package.json`). `verify-columns.js`: cruza columnas usadas en queries Supabase de `js/` contra esquema real (58 tablas en `schema-snapshot.json`; ignora joins embebidos); detecta "columna inexistente". Resultado actual: **0 mismatches**. `--refresh` regenera snapshot vía Management API. `npm run verify:columns` + `verify:planner`. Job `static-checks` en CI semanal: sintaxis JS + `verify:columns` + `verify:planner` (sin secretos).
- `2026-06-16` · `40c90ce` — test(rls): **setup automatizado cuenta familia de prueba** (`scripts/setup-test-users.js`). `npm run setup:test-users` (con `SUPABASE_SERVICE_ROLE_KEY`): crea `test-familia-agora@didactia.eu` (rol familia, Agora), la vincula a un alumno con calificaciones, imprime `TEST_FAMILIA_AGORA_EMAIL/PASSWORD` para `.env`. Desbloquea `npm run test:rls-familia`.
- `2026-06-16` · `4d81205` — fix(qa): **handlers rotos — JSON.stringify legacy + apóstrofo en nombre** (`js/familias.js` + `js/mejoras.js`). `mejoras.js` `loadDashboard`: última instancia de `JSON.stringify` sin escapar en `onclick` → envuelta con `_mhEsc`. `familias.js` botón comedor: argumento con `&#39;` que el navegador decodifica a `'` ANTES de JS → rompía nombres con apóstrofo (O'Brien); además el argumento era innecesario → eliminado.
- `2026-06-16` · `bc8d96c` — fix(css): **regla .pill/.pill-active para chips de filtro de Alumnos** (`css/styles.css`). Chips de grupo en directorio Alumnos (`class="pill"/"pill-active"` en `.al-pills`) salían como botones por defecto del navegador. Nueva regla scopeada `.al-pills .pill` con tokens design system v2 (inactivo `paper-2/line`; activo `ink/blanco`).
- `2026-06-16` · `9358a8e` — feat(asistencia): **informe/estadísticas de asistencia de aula por alumno** (`js/asistencia.js`). Toggle "Pasar lista / Informe" en `#panel-pasarlista`. Modo **Informe** (`_asistInformeCargar`): rango de fechas (últimos 30 días por defecto) + selector de grupo (dirección) / grupos del profesor (match por tokens). Tabla por alumno: presente/retraso/ausente/total/% asistencia, ordenada por ausencias desc; % coloreado (verde ≥90, ámbar ≥75, rojo <75). KPIs: registros, ausencias y retrasos totales, alumnos con ≥3 ausencias. Export Excel (`_asistInformeExportar`, SheetJS). `_asistRenderLista`/`_asistRenderInforme` + helpers `_asistPoblarGrupos`/`_asistGruposDelProfesor`.
- `2026-06-16` · `03d2644` — feat(asistencia): **vista "Pasar lista" por fecha** (`js/asistencia.js` + `app.html`). Nueva pestaña `#panel-pasarlista` (grupo Docencia, visible profesional/admin/etc., `initAsistenciaVista`): selector de día + selector de grupo (dirección) / grupos del profesor (match por tokens). Lista de clases del día con resumen ✓/⚡/✗ desde `asistencia_clase` y botón "Pasar lista" que abre el modal con la fecha elegida. `_asistOnClose` refresca. `_aEnsureStyles` inyecta estilos una vez (estaban solo dentro del modal → `.hh-btn-lista` sin estilo antes del primer open). Cableado: `tab/nav/panel/TAB_MAP/TITLES/MAS_CFG/patch showTab` + `auth.js`.
- `2026-06-16` · `6015db1` — feat(asistencia): **pasar lista de clases ya concluidas (mismo día)** (`js/mejoras.js`). El botón "Pasar lista" en "Mi horario de hoy" ya no requiere que la clase esté activa AHORA — aparece en cualquier clase de hoy que ya haya comenzado. En clases futuras del día no se muestra. Etiqueta: "Pasar lista" (clase activa) o "Pasar lista (atrasada)" (ya terminada).
- `2026-06-16` · `ebb1463` — fix: **botón "Pasar lista" y handlers rotos por JSON.stringify en onclick** (`js/asistencia.js` + `js/incidencias.js` + `js/mejoras.js`). `JSON.stringify` de un string produce comillas dobles que, dentro de `onclick="…"`, parten el atributo. Fix: `mejoras.js` → `_argAttr()` con comillas simples escapadas; `asistencia.js` → `_aEsc()` (→ `&quot;`) en botones del modal; `incidencias.js` → `esc()` en botón "📧 Familia".
- `2026-06-16` · `c25c95d` — docs(roadmap): **fase 3 RLS confirmada aplicada en producción** (`CLAUDE-ROADMAP.md`). Verificado vía Management API: las 10 políticas de `rls_familia_lockdown_fase3` existen en BD y las permisivas antiguas ya no. Línea de roadmap actualizada de "pendiente ejecutar" a "aplicado y verificado".
- `2026-06-16` · `85bce6d` — feat(redesign): **cabeceras Newsreader en paneles Planner y Análisis** (`app.html` + `css/styles.css`). Planner: header fijo (eyebrow DOCENCIA·HERRAMIENTAS + h1 30px Newsreader + subtítulo) separado del contenido scrollable; tabs como `.tabs-in-hdr` sin borde propio; banner `planner-tramos-warn` migrado a tokens `var(--warning-soft)/var(--warning)`. Análisis/CMI: header fijo (eyebrow DIRECCIÓN·ANÁLISIS + h1 30px + subtítulo); tabs con iconos 📊/📄; CSS `#analytics-container .cmi-hdr > div:first-child { display:none }` oculta el título duplicado JS-rendered y convierte `.cmi-hdr` en toolbar de botones. `planner-wrap`: `flex:1;min-height:0` para scroll correcto. Nueva clase `.tabs-in-hdr`.
- `2026-06-16` · `2038d33` — feat(planner): **Sprint 4 — cobertura del horario** (`js/planner.js`). `_calcCobertura()`: cuenta sesiones colocadas en `_s.schedule` por `(grupo, materia_id)` y compara con `_s.necesidades`; devuelve `{items, porGrupo, totalItems, okItems}` con estado ok/warn/error por necesidad y por grupo. `plannerCobertura()`: modal overlay con tabla por grupo (color dot por materia, req/gen/estado) y resumen global. Tablero: selector de grupo muestra badges `✓`/`⚠ Xh/Yh`/`❌` de cobertura calculada antes del render; botón "📊 Cobertura" añadido a la cabecera del tablero (junto a Verificar). Tab Publicar: banner verde "✅ Todas las necesidades cubiertas" o ámbar "⚠️ X/Y cubiertas — botón Ver detalles →" (IIFE en el innerHTML para no contaminar el scope).
- `2026-06-15` · `c29d6b0` — fix: **flex-direction:column en paneles materiales, orientacion y calidad** (`app.html`). Todos tenían el `flex-direction:column` solo en el `div` interno, no en el `.panel` raíz — cuando `.panel.active` activa `display:flex`, el default `row` hacía que el wrapper solo ocupara el ancho del contenido (media pantalla vacía). Mismo patrón roto que comedor (fix `f78f661`). También cambia `height:100%` → `flex:1` en el wrapper interno para que el scroll funcione correctamente.
- `2026-06-15` · `28b9753` — feat(calificaciones): **split layout admin** (`js/calificaciones.js` + `css/styles.css` + `app.html`). Panel admin bifurcado: `.cal-list-panel` (58%, izquierda) con filtros compactos + tabla clickeable + notas coloreadas (rojo <5 / verde ≥5); `.cal-detail-panel` (flex:1, derecha) con placeholder → al clicar fila: `_calClicarFila` → `_calAbrirAlumno(nombre, grupo)` → stat de media general + tabla pivote asignatura×evaluación. `_calSelectedAlumno` persiste selección entre re-renders. Vistas profesor/familia con scroll propio en el nuevo contenedor. CSS: `.cal-list-panel`, `.cal-filtros-wrap`, `.cal-list-scroll`, `.cal-detail-panel`, `.cal-alumno-empty`; hover/sel rows; responsive ≤900px oculta detalle.
- `2026-06-15` · `f78f661` — fix(comedor): **flex-direction:column en #panel-comedor** (`app.html`). El panel mostraba media pantalla vacía porque `display:flex` del `.panel.active` usaba `flex-direction:row` por defecto. Fix: `style="flex-direction:column"` en el div raíz del panel.
- `2026-06-15` · `e83a655` — feat(incidencias): **clic en fila abre panel de detalle** (`js/incidencias.js` + `css/styles.css`). El redesign `89c0553` creó el shell `.inc-split-view` pero no cableó el click. Fix: `<tr onclick="_incAbrirDetalle(id)">` + `event.stopPropagation()` en los botones de acción para que no propagen. `_incAbrirDetalle(id)`: resalta fila (`inc-row-sel`), construye panel derecho con badges gravedad/estado, grid de metadatos, descripción, informe borrador, normativa, medidas y botones de acción. `_incDetalleAccion(accion, id)`: BD update + refresca lista + recarga detalle. `_incLastData`: cachea datos de la última carga. `_incSelectedId`: persiste ID seleccionado para re-resaltado tras filtrar.
- `2026-06-15` · `b69ae0e` — feat(alumnos): **redesign split layout** (`js/alumnos.js` + `css/styles.css`). Reescritura completa: layout `.al-split` (grid 1.35fr/1fr); lista con avatares iniciales+color determinístico, buscador, chips de grupo, fila seleccionada tint `var(--ink)`; drawer con avatar 52px + nombre Newsreader + 3 tabs (Perfil/Horario/Actividad); bloques async fire-and-forget (familias, horario mini-grid, comedor dots, incidencias, calificaciones pivote); footer PDF+incidencia; clic en fila actualiza drawer sin reemplazar panel; `_alSelectedId` persiste entre búsquedas. Responsive stack ≤768px. 330 líneas CSS `.al-*` nuevas.
- `2026-06-15` · `4c869b7` — fix: **agente sustituciones + topbar centro**. `agent-sustituciones`: filter añade `cubierta=true && sin_sustituto`; EF redesplegada. `auth.js` `updateUI()`: escribe `#topbar-ctr-name` directo en DOM (antes `updateBentoDashboard` en IIFE nunca se ejecutaba desde `auth.js`).
- `2026-06-15` · `b124a72` — feat(familia): **portal familiar rediseñado** (`app.html` + `css/styles.css` + `js/mejoras.js`). Hero card `#fam-child-card`: avatar con inicial del nombre (tematizado `--ink`), nombre completo del hijo activo y grupo/curso — se popula desde `renderHomeFamilia` y se actualiza al cambiar de hijo. Quick nav `.fam-quick-nav`: 4 botones de acceso directo (Comunicados, Notas, Salidas, Asistente) en grid 4 cols, sin JS adicional. Reorden de bloques: Horario de hoy ahora es el primero (bloque más relevante del día) por delante de Avisos. Botón "Avisar que no come mañana" usa `.fam-comedor-action` (color `--ink`, border-radius 10px) en lugar de `btn btn-s` con inline styles. "Ver todos los comunicados →" usa `.fam-ver-mas` (color `--ink`, sin borde). Incidencias mueve al final (es el bloque menos urgente para las familias). Nuevos tokens CSS: `.fam-child-card/.fam-child-avatar/.fam-child-name/.fam-child-meta`, `.fam-quick-nav/.fam-qnav-btn/.fam-qnav-icon`, `.fam-comedor-action`, `.fam-ver-mas`; responsive `@media (max-width:480px)` para nav en 2 filas y padding reducido.
- `2026-06-15` · `690973c` — fix(chat): **EF chat** — renombrar `añadir_clase` → `agregar_clase` (Gemini rechaza nombres con caracteres no-ASCII). Verificado que el resto de herramientas no tienen caracteres fuera del rango `[a-zA-Z0-9_.-]`. EF redesplegada a producción.
- `2026-06-15` — fix(seguridad RGPD): **auditoría RLS fase 3** (`supabase/migrations/rls_familia_lockdown_fase3.sql`, ⚠️ pendiente ejecutar en SQL Editor). Tablas con política `FOR ALL centro_id` que permitían a `familia` INSERT/UPDATE/DELETE por API REST directa, y leer datos de todos los alumnos del centro. Correcciones: `sustituciones` (familia: SELECT solo grupos de sus hijos; antes podía modificar sustituciones); `comunicados` (familia: SELECT solo `estado=enviado` y `destinatarios != solo_profesores`; antes veía comunicados internos de staff); `salidas_didacticas` (familia: SELECT solo `estado=publicada`; antes podía crear/borrar salidas); `participantes_salida` (familia: SELECT+UPDATE solo sus hijos; antes veía datos de todos los alumnos); `notificaciones_salida` (staff-only). Test e2e `tests/rls-familia.spec.js` ampliado con pasos 7–10: INSERT bloqueado, comunicados sin `solo_profesores`, salidas sin borradores.
- `2026-06-14 22:56` · `89c0553` — feat(redesign): redesign visual 3 paneles — Asistente IA, Sustituciones, Incidencias **módulo control de asistencia de aula** (`js/asistencia.js` nuevo + mejoras.js + analytics.js + app.html). Tabla `asistencia_clase` ejecutada vía Management API. Botón 📋 en clase activa AHORA en Mi horario de hoy. Modal fullscreen: alumnos del grupo con toggle Presente/Retraso/Ausente + observación; UPSERT inmediato; push a familias al marcar ausente (inmediato) o retraso (al confirmar). CMI: 4ª métrica Ausencias de aula hoy en Dimensión 1 Operativa.
- `2026-06-12 23:22` · `3fac06d` — docs(CLAUDE.md): sesión 2026-06-12 — sistema alergias/dietas documentado
- `2026-06-12 22:47` · `8aa7436` — feat(comedor): sistema unificado alergias/dieta — perfil permanente + nota diaria + pre-relleno salidas
- `2026-06-12 21:13` · `115a95b` — fix: resolver conflicto CLAUDE.md + deploy notify-sustitucion documentado
- `2026-06-12` — fix(seguridad RGPD): **auditoría RLS — fuga sistémica a familias**. El patrón `centro_id = mi_centro` en políticas de SELECT/ALL dejaba a las cuentas `familia` (que tienen `centro_id`) leer por API datos sensibles de TODOS los alumnos. **Fase 1 aplicada** (`rls_familia_lockdown_fase1.sql`): 7 tablas sin lectura de familia → staff-only (orientación clínica: informes/medidas/cuestionarios/alertas; alertas_predictivas; calidad: NC/CAPA). **Fase 2 aplicada** (`rls_familia_lockdown_fase2.sql`): expedientes/tramites_orientacion + incidencias → staff-only con RPCs `SECURITY DEFINER` (`familia_tramites_visibles`/`familia_incidencias_hijos`) para que las familias vean solo lo suyo (la RLS no filtra columnas → el RPC evita exponer `notas_internas`/`informe_borrador`); `feedback_familias` → propio. JS acoplado desplegado (`oriRenderTramitesFamilia` y bloque incidencias de `renderHomeFamilia` ahora llaman al RPC).
- `2026-06-12` — fix(seguridad)+feat(calificaciones): **calificaciones para familias (solo lectura) + cierre de fuga RGPD**. Hallazgo: la política `cal_read` permitía a cualquier usuario del centro (incl. `familia`) leer TODAS las calificaciones por API. Migración `supabase/migrations/calificaciones_familia_rls.sql` (✅ **aplicada y verificada en producción 2026-06-12**): staff lee su centro; familia solo `alumno_id ∈ familia_alumno(profile_id=auth.uid())`; superadmin todo. Vista familia en `js/calificaciones.js` (`_calRenderFamilia`: tabla pivote asignatura×evaluación, selector de hijo si >1, solo lectura). Tab Calificaciones visible para `familia` en `auth.js`.
- `2026-06-12` — fix(alumnos): familias vinculadas leídas en dos pasos (sin embed FK `familia_alumno→profiles`).
- `2026-06-12` — feat(alumnos): **módulo Alumnos nuevo** (`js/alumnos.js`). Directorio del centro: `initAlumnos()` (lista + buscador por nombre + chips de filtro por grupo, contador, **export Excel** `_alExportar` de la lista filtrada con SheetJS). Ficha individual `alumnosAbrirFicha(id)` con 5 bloques async fire-and-forget: familias vinculadas (`familia_alumno→profiles`), horario semanal por día (`horarios_grupo` por grupo+curso), comedor últimos 14 días lectivos, incidencias (`ilike alumno_nombre`), calificaciones (`calificaciones` por `alumno_id`). XSS: `_alEsc`; argumentos onclick seguros: `_alArg` (evita romper el atributo con UUIDs/grupos). **Integración ⌘K**: el command palette (`js/palette.js`) al elegir un alumno abre su ficha vía `window.alumnosVerFicha(id)` para staff (familia mantiene fallback al chatbot); `alumnosAbrirFicha` hace fetch del alumno por id si la lista no estaba cargada (apertura directa). **Acción "Registrar incidencia"** desde la ficha (`alumnosRegistrarIncidencia`, solo admin/admin_institucional/superadmin = visibilidad real del tab Incidencias): navega a Incidencias y prerrellena `inc-alumno`/`inc-grupo` (reintenta hasta que la vista admin renderice). **Export PDF de la ficha** (`alumnosExportarFichaPDF`, botón "⬇ PDF" para cualquier rol que ve la ficha): reutiliza los helpers globales de `informes.js` (`_infEnsureLibs`/`_infCentroInfo`/`_infImgToDataURL`/`_infHexToRgb`) → jsPDF + autotable con cabecera logo+color del centro; 5 secciones (familias, horario semanal, comedor 30d, incidencias, calificaciones), una fila "Sin registros" por sección vacía. Cableado en `app.html` (`tab-alumnos`/`nav-alumnos` grupo Docencia/`panel-alumnos`/TAB_MAP/MAS_CFG/TITLES/patch showTab/script) + `auth.js` (visibilidad: profesional/admin/admin_institucional/superadmin/director/jefatura/orientador, **no familia**). También: `syncGroupLabels()` oculta encabezados de grupo del sidebar sin items visibles.
- `2026-06-12` — feat(familia): **home consolidado como portal** (`renderHomeFamilia` en `js/mejoras.js`). Bloque "Avisos" (aviso del centro + cambios de clase hoy, antes huérfano en `panel-avisos`) + comedor accionable ("avisar que no come mañana", `window._fhAvisoManana`).
- `2026-06-12` — feat: **módulos siempre disponibles para todos los centros (excepto IB)** (`config.js` `_conModulosBase`, `auth.js`, `users.js`, `app.html`). Eliminados los toggles de módulos del panel Usuarios. Limpieza: `.gitignore` ignora `repomix-output.xml` y `edubot-supabase (1).html` (este último sacado del index).
- `2026-06-12 07:40` · `938602f` — docs(CLAUDE.md): P1 EFs versionadas — marcar completado + nota rapid-processor/cas-analyzer
- `2026-06-12` — fix: deploy `notify-sustitucion` a producción (estaba versionada pero no deployada)
- `2026-06-12 07:26` · `c232c38` — feat: versionar EFs `invite-user` y `notify-role` — código extraído del bundle ESZIP de producción vía Management API. `notify-sustitucion` ya estaba en el repo (sin deploy activo). Funciones en prod no versionadas aún: `rapid-processor`, `cas-analyzer`.
- `2026-06-12 01:16` · `a0ca561` — docs(CLAUDE.md): home familia documentada — renderHomeFamilia, VAPID fix
- `2026-06-12 00:56` · `967a728` — feat(familia): home mejorada — `renderHomeFamilia()` en `mejoras.js`; 5 bloques fire-and-forget (horario hoy, comedor, incidencias, salidas, comunicados no leídos); selector de hijo con chips; render target `#familia-home-content` en `app.html`; fix `const VAPID_PUBLIC_KEY` duplicado (eliminado de `mejoras.js`, permanece solo en `config.js`)
- `2026-06-11 23:50` · `6fd3497` — fix: VAPID_PUBLIC_KEY real en config.js — par regenerado, secrets actualizados, send-push redesplegada
- `2026-06-11 23:36` · `1e1302b` — docs: agent-sustituciones + push familias documentados en roadmap
- `2026-06-11` — feat(push-familias): **suscripción a notificaciones push para familias** (`js/familias.js`, `js/auth.js`, `js/config.js`). `initPushFamilias()` llamada tras login de rol familia: comprueba soporte Push API, consulta si ya suscrito, muestra banner `#push-banner-familia` con botón Activar y ✕ persistente (localStorage). `_pushActivar()`: SW ready → `pushManager.subscribe` con VAPID → INSERT en `push_subscriptions` fire-and-forget → toast confirmación. `VAPID_PUBLIC_KEY` en `config.js` (TODO pendiente de sustituir con valor real). SW ya tenía handler push completo — sin cambios.
- `2026-06-11` — feat(agent-sustituciones): **primer agente IA autónomo** (`supabase/functions/agent-sustituciones/index.ts`). Edge Function con Gemini function calling y 3 herramientas encadenadas: `obtener_ausencias_sin_cubrir` (sustituciones sin cubrir hoy), `buscar_profesores_libres` (cruza `horarios_grupo` con profesores del centro), `sugerir_sustituto` (ordena por equidad trimestral). El agente solo actúa cuando hay ausencias reales — si no hay, responde "No hay ausencias sin cubrir hoy." Integrado en la home del rol admin/director/jefatura como bloque "🤖 Resumen de guardias" (fire-and-forget, no bloquea carga). P0 seguridad `disponibilidad_profesor` cerrado (aislamiento garantizado por RLS + filtro cliente, no requería cambio).
- `2026-06-11 10:27` · `d969c51` — Merge branch 'main' of https://github.com/jesvivlc/DidactIA
- `2026-06-12` — feat(agent): detalle adicional de `agent-sustituciones` — el `centro_id` es SIEMPRE el del body (nunca el del modelo), `.eq("centro_id", …)` en las 3 queries, log por herramienta; herramientas dedup por nombre normalizado y filtro de PENDIENTE / nombres con `?`/`/`; `sugerir_sustituto` top 3; integración UI: botón "🤖 Buscar profesor libre" en `#sust-vista-admin` (`buscarProfesorLibreAgente`) + briefing `_renderAgentBriefing` en home admin.
- `2026-06-11 00:54` · `81de9b4` — docs(CLAUDE.md): sesión 2026-06-11 — Calidad NCs+CAPA+Feedback, roles orientador/admin_institucional, tablas calidad
- `2026-06-11` — feat(orientación): **notificaciones push + UX responsive** (`js/orientacion.js`). Push vía EF `send-push` (helper `_oriPush`, silencioso/no bloqueante) en 4 momentos: cuestionario completado→orientador; nueva alerta (manual y detección IA)→orientador/jefatura/director/admin; informe validado→director/admin; trámite `visible_familia`→familias (`familia_alumno.profile_id`). Responsive (CSS inyectado una vez, sin tocar `styles.css`): tabla→tarjetas en <768px, pestañas→`<select>`, modales 95%+×, FAB "Nuevo expediente", ellipsis en celdas.
- `2026-06-11` — feat(orientación): **exportación RGPD + estadísticas** (`js/orientacion.js`). "📥 Exportar expediente completo" (role-gated admin/director/jefatura/superadmin) → PDF único (jsPDF+autotable) con portada (logo+color, nota legal Art. 15 RGPD/LOPDGDD), medidas, informes (texto completo), observaciones docentes, trámites, alertas + pie/nº página + nota de cierre. "📈 Estadísticas": 6 bloques (4 tarjetas, barras por medida, línea alertas 6m por nivel, donut estado informes, top-10 cuestionarios pendientes, trámites por estado) con Chart.js + export PDF (tablas) y Excel (6 hojas).
- `2026-06-11` — feat(orientación): **IA + panel de riesgo + portal familias** (`js/orientacion.js`). Informes: "✨ Generar borrador con IA" (LOMLOE, vuelca sin guardar). Adaptaciones: "✨ Sintetizar observaciones docentes" (≥2 cuestionarios). Panel de riesgo: alertas activas ordenadas, alerta manual y "✨ Detectar riesgos automáticamente" (asistencia_comedor+sustituciones 30d→Gemini JSON→checkboxes→INSERT confirmados). Portal familias: sección autocontenida en la home (rol familia) con trámites `visible_familia=true`. IA vía EF `chat` con `role:"familia"` (texto puro); ninguna IA persiste sin acción manual.
- `2026-06-11` — feat(orientación): **expediente individual con 4 pestañas** (`oriAbrirExpediente`). Resumen (ficha + medidas activas + alertas + trámites), Informes (Ver/Editar con guardia firmado=solo lectura, Nuevo, Exportar PDF), Adaptaciones (medidas detalladas + cuestionarios docentes Rellenar/Ver/Enviar), Trámites (filtro + editar + badge visible familia).
- `2026-06-11` — feat(orientación): **módulo nuevo + tablas** (`js/orientacion.js`, `supabase/migrations/orientacion_base.sql` ejecutado vía Management API). 6 tablas + RLS por centro. Lista de expedientes (JOIN alumno+orientador, medida más grave activa, nivel de riesgo, filtros, contador, export XLSX, modal nuevo expediente). Nav item "🧭 Orientación" (grupo Centro, visible orientador/jefatura/director/admin/superadmin).
- `2026-06-10` — feat(login): **logo y color del centro en la pantalla de login** (antes de autenticar). `themeLoginScreen()` (en `js/auth.js`, llamada desde el boot de `js/config.js` tras crear el cliente) detecta el centro por la URL —`?centro=<uuid>` (por `id`) o `?centro=`/`?c=`/`?codigo=` con un código de acceso (cruza `codigo_familia`/`codigo_profesional`/`codigo_acceso`)— y aplica `applyTheme(color_primario, logo_url)`. Fallback: sin pista en URL → última marca usada en este navegador (`localStorage didactia_brand`, persistida por `_cacheBrand` tras login); sin nada → marca DidactIA por defecto.
- `2026-06-10` — feat(analisis): **fusión Analytics CMI + Informes en un módulo único "📊 Análisis"** (`app.html` + `js/auth.js`; `analytics.js`/`informes.js` intactos). Un único nav item (grupo Administración, visible admin/director/jefatura/superadmin) y `#panel-analisis` con dos pills internas: **Dashboard** (contenedor `#analytics-container`) e **Informes PDF** (`#inf-container`). `analisisTab()`/`initAnalisis()` conmutan visibilidad y hacen lazy init del sub-módulo activo. Eliminados nav/tab/panel separados de Analytics e Informes.
- `2026-06-09` · **Multi-curso horarios_grupo** — `curso_escolar TEXT NOT NULL DEFAULT '2025-26'` + `info_centro.curso_activo`; global `cursoActivo` en `config.js`; auth.js carga `curso_activo` tras login (fire-and-forget); 7 queries `chat.js` + 4 `admin.js` filtran por `curso_escolar`; Planner Publicar: selector 2025-26/2026-27, aviso si es el curso activo, DELETE filtra por `centro_id+curso_escolar+grupo_horario`. Migración `horarios_curso_escolar.sql` ✅ aplicada y verificada en producción (2026-06-12).
- `2026-06-09` · **Planner: HC-MATERIA-DIA en H-MRV-SA** — `TimetableSolver` añade `subjectDayOccupancy` y `maxPerDay=ceil(h/5)` en constructor; `getValidSlots` las comprueba; `assignBlock`/`removeBlock` las mantienen sincronizadas.
- `2026-06-09` · **Planner fix: slots sin profesor_id** — `plannerElegirVariante` construía slots sin `profesor_id`, `materia_id`, `codocente_prof_ids` → `_slotSinProfesor()` = true → "⚠ Sin asignar" en todas las celdas. Fix: extraer `tIds=item.teacherIds`, poblar los tres campos + `sin_asignar`.
- `2026-06-09` · **Agora tramos 26-27** — 10 tramos cargados vía Management API: 8 lectivos (08:50–17:00) + Recreo (11:20–11:50) + Comida (14:20–15:20). Anteriores 7 genéricos borrados.
- `2026-06-09` · **`scripts/importar_cargas_eso.mjs`** — importa `data/Cargas_ESO_limpia.xlsx` hoja "Cargas ESO" para 1ºESO A/B/C + 3ºESO A/B/C. Resultado: 75 filas, 0 nulls.
- `2026-06-09` · `a4684f1` — feat(informes): módulo **Informes de dirección** (`js/informes.js`). Selector de periodo → **PDF consolidado** con jsPDF + jspdf-autotable (CDN on-demand). 6 secciones (Resumen, Sustituciones, Ausencias RRHH, Guardias, Incidencias, Comedor). SW `v5→v6`.
- `2026-06-09` · `4cfcc31`/`b9bf71c` — feat(materiales): form de subida **multi-grupo** (`<select multiple>`) + **"solo mis clases"** (match nombre normalizado del profesor contra `profesores.nombre`).
- `2026-06-09` · `dc9f12c`/`0c225fd` — feat(materiales): módulo **hub de materiales** por grupo/asignatura (`js/materiales.js`, tabla `materiales` + bucket privado `materiales`, `supabase/migrations/materiales.sql` ejecutado). SW `v4→v5`.
- `2026-06-09` · `2d705b1` — feat(planner): la hoja **"Cargas"** del importador deja de ir a `planner_cargas` y se vuelca a `materias + profesores + necesidades_lectivas` (con `centro_id=ctrId`). (`_impCargasANecesidades`). Tabla extra `planner_grupos` para la hoja Grupos.
- `2026-06-09` · `21bf059` — feat(planner): modelo de datos de **entrada del generador** + importador. 7 tablas (`planner_profesores`, `planner_cargas`, `planner_tramos`, `planner_restricciones`, `planner_disponibilidad`, `planner_espacios`, `planner_reglas`). Nueva sub-pestaña **Importar** del Planner.
- `2026-06-09` · `e38176e` — feat(calificaciones): módulo gradebook nuevo (`js/calificaciones.js`). Tabla `calificaciones` + RLS (`supabase/migrations/calificaciones.sql`, ejecutado). Vista profesor: grupo/asignatura/evaluación → tabla editable. Vista dirección: filtros + tabla solo lectura + export CSV y PDF. SW `v3→v4`.
- `2026-06-05` · Asistente IA como **burbuja flotante** + **sidebar rediseñado**. El chat sale del rail fijo a un **overlay deslizante** (`#asistente-overlay`/`.asist-panel`, derecha); **burbuja FAB** (`#asistente-fab`, 56px, color `--ink` del centro) abajo-derecha + globo de invitación. Sidebar agrupado: Inicio + Asistente (sin cabecera) · grupos **DOCENCIA** y **CENTRO** · iconos 19px con color por familia (`--nav-color`), activo = `color-mix` tintado.
- `2026-06-05` · Home rediseñada (6 cambios) — cabecera compacta + buscador protagonista; topbar minimalista (logo+centro+lupa+avatar); bloque "Mi horario de hoy" (`renderMiHorarioHoy`); métricas accionables (`renderHomeMetrics`); eliminados grids "Módulos"/"Acceso rápido"; command palette global ⌘K (`js/palette.js`).
- `2026-06-05` · **Incidencias** — vistas diferenciadas por rol: admin ve pantalla completa; profesional ve formulario simple + historial readonly. `initIncidenciasPanel()` bifurca a `#inc-vista-admin` / `#inc-vista-profesor`.
- `2026-06-05` · **Flujo unificado ausencias** — formulario único en Sustituciones (vista profesional). `notificarAusenciaProfesor()` dual-write: INSERT `ausencias_profesor` (admin) + INSERT `sustituciones` por cada día lectivo (operativo) con `§RRHH§{ausencia_id}` en observaciones.
- `2026-06-05` · **Sustituciones notificaciones** — EF `notify-sustitucion`: email al ausente ("cubierta por X") + email al sustituto (grupo, tramo, instrucciones) al asignar. EF `notify-justificante`: recordatorio 48h sin justificante. Cron pg_cron `notify-justificante-daily` `0 8 * * *`.
- `2026-06-05` · **Migration** — `20260605002_sustituciones_sustituto_nullable.sql`: `profesor_sustituto DROP NOT NULL`. Aplicada vía Management API.
- `2026-06-05` · **Planner fallback** — `TRAMOS_DEFAULT` genérico + banner `#planner-tramos-warn`.
- `2026-06-05` · **Comedor contextual** — `_detectarContextoProfesor()` usa `tramos_centro` + `horarios_grupo` para detectar clase activa.
- `2026-06-04` · Exportaciones — SheetJS (xlsx 0.18.5) cargado en `app.html`. **Planner**: botones PDF y Excel. **Sustituciones/Comedor/Incidencias**: CSV → .xlsx. **RRHH**: nuevo botón Exportar → .xlsx. **Guardias**: nuevo botón Exportar → .xlsx.
- `2026-06-04` · Planner — horarios SIN profesor asignado: (1) `horario_generado.profesor_id` nullable; (2) modo CSP "sin profesores"; (3) Tablero: slots rayados "Sin asignar", modal selector + panel lateral de profesores arrastrables; (4) chat: `asignar_profesor` + `asignar_profesor_materia` (masivo).
- `2026-06-04` · Planner Tablero — drag & drop con validación de hard constraints al soltar (`_ejecutarDrop` simular-validar-revertir) + zona "Aparcados" persistida en localStorage.
- `2026-06-04` · EF `chat` — 4 herramientas Gemini para editar `horario_generado` del Planner sin regenerar: `mover_clase`, `eliminar_clase`, `añadir_clase`, `cambiar_profesor`. ✅ Desplegada a producción.
- `2026-06-04` · `9b0e81d` — feat(planner): hard constraints universales HC-VENTANA + HC-INICIO-FIN en `_esHardValido()` (V2); `scripts/verify-hard-constraints.js`
- `2026-06-03` · Supabase deploy — EF `chat` redesplegada a producción (7 herramientas, +tramos)
- `2026-06-03 20:27` · `dbea96e` — fix: banner bienvenida siempre muestra el centro activo
- `2026-06-03` · EF `chat` — herramientas `crear_tramos_centro` y `listar_tramos_centro` implementadas y desplegadas a producción
- `2026-06-03 00:03` · `528eeb2` — test: Playwright e2e — aislamiento multi-tenant RLS entre Agora y Buñol
- `2026-05-29 22:53` · `aedc22f` — fix: EF chat — try/catch con console.error en Path A confirm_tool
- `2026-05-29` · `a3ec38f` — fix: sustitucion busca horario profesor automaticamente
- `2026-05-29` · `ce6663a` — feat: chatbot — sustitucion activa esConsultaHorario + inyecta contexto horario a Gemini
- `2026-05-29` · `4c024f0` — feat: chatbot — detectar tramos ordinales en extractDiaHora (tercera hora → 10:40)
- `2026-05-29` · `383f2a9` — feat: chatbot — guardar contexto último profesor entre turnos (_ultimoProfesor)
- `2026-05-29` · `d3bbb4b` — fix: chatbot — búsqueda profesor tolerante a tildes, stopwords y diminutivos (Salva→Salvador)
- `2026-05-29` · `6ace69a` — fix: 5 edge cases — selector sustitutos usa fecha formulario, guard re-aprobación, tab IB para profesional, límite comunicados 500, superadmin sin centros muestra mensaje
- `2026-05-29` · `f1e1582` — fix: 4 issues de seguridad — contraseña en _regPass, SB_URL en notify-role, sanitizeReply Gemini, JSON.stringify en onclick alumnos
- `2026-05-29` · `70960c3` — fix: 8 bugs moderados — RRHH (privacidad motivo, null sustituto, ilike nombre), admin (badge sin cubrir, filtro al eliminar), incidencias (cache por ctrId), comunicados (grupo vacío), usuarios (eliminar solo pendientes)
- `2026-05-29` · `d7c654a` — fix: 5 bugs críticos — comedor (horaActual, toggleAsistencia fecha, filtro grupo, histórico limit), guardias (ilike wildcards)
- `2026-05-29 01:01` · `f87dcf8` — fix: chatbot agente ejecutor — systemInstruction + toolInstr sin confirmación
- `2026-05-29 00:51` · `5ce7a90` — feat: dashboard Classroom-style — module cards, metrics pills, sidebar tint
- `2026-05-29 00:11` · `5d7cf08` — feat: chatbot agente ejecutor — function calling con confirmación UI
- `2026-05-29` · Supabase deploy — EF `chat` desplegada con function calling (5 herramientas)
- `2026-05-28 14:47` · `bf2a3fc` — feat: CMI Analytics — Cuadro de Mando Integral con alertas predictivas
- `2026-05-28 14:22` · `48d624b` — feat: Planner — H-MRV-SA UI connection (progreso, variantes, diagnóstico)
- `2026-05-28 06:48` · `02420d1` — feat: motor H-MRV-SA — TimetableSolver + DidactIAPlanner (motor puro)
- `2026-05-28 01:06` · `a44a77a` — feat: Planner — tab Dictar con IA multimodal (voz/texto/audio) + EF parse-restricciones desplegada
- `2026-05-28 00:48` · `90bb22b` — feat: planner — tooltip LOMLOE + configuración de tramos horarios
- `2026-05-27 23:45` · `54827f4` — docs: CLAUDE.md actualización 2026-05-27 — módulo Planner completo
- `2026-05-26 06:55` · `255a1b2` — feat: DidactIA Planner — generador de horarios con CSP + drag & drop
- `2026-05-26 00:53` · `5afc6b3` — feat: inicio admin layout — stat2 tiles, AI rail, sidebar SVG brand, topbar v2
- `2026-05-25 20:27` · `e9e9652` — feat: design system v2 — warm editorial tokens + layout shell redesign
- `2026-05-25 16:57` · `182b449` — feat: migrar navegación a sidebar lateral con topbar y bottom nav móvil
- `2026-05-24 23:02` · `e755e36` — feat: módulo comunicados internos con envío por email y realtime
- `2026-05-24 22:52` · `68b7a46` — feat: flujo convivencia completo — informe editable + notificación jefatura
- `2026-05-24 21:43` · `e8089d4` — feat: tipificar-incidencia con normativas de todas las CCAA
- `2026-05-24 20:52` · `059f39a` — feat: mejoras módulo incidencias — buscador alumno, filtro grupo, CSV, contador tab
- `2026-05-23 17:44` · `bf718d3` — feat: SQL centro demo IES Demo con datos completos para ventas
- `2026-05-23 12:14` · `1484aab` — feat: n8n alertas plazos IB + tabla plazos_ib
- `2026-05-23 12:07` · `52ce5c3` — feat: n8n alerta absentismo comedor
- `2026-05-23 11:53` · `cd592dc` — feat: n8n informe semanal automático para dirección
- `2026-05-22` · `e56ccb2` — feat: `js/guardias.js` — bolsa de guardias con equidad por trimestre
- `2026-05-22` · `15ef7d8` — feat: `js/rrhh.js` — módulo RRHH completo (ausencias, aprobación, rechazo)
- `2026-05-22` · `049c9a1` — feat: `js/users.js` reescritura completa — panel admin+superadmin, modales invitar/editar, desactivar/reactivar, reenvío invitación
