# DidactIA — Registro de cambios

> Referenciado desde CLAUDE-ROADMAP.md. Ver también @CLAUDE-MODULOS.md.
>
> **Rotación:** este archivo mantiene solo las entradas recientes (objetivo ≤30 entradas / ≤20k chars). Las anteriores se mueven a `CLAUDE-ARCHIVE.md` (no cargado por Claude Code). Última rotación: 2026-06-21 (archivadas las entradas anteriores a 2026-06-19). Protocolo en CLAUDE.md → "Mantenimiento de la documentación".

## Registro de cambios recientes

- `2026-06-21` · `518561f` — feat(calificaciones): **auto-comentario competencial al cambiar el nivel** (`js/calificaciones.js`). `_calCompAutoComment(idx)` — calcula la media de los niveles seleccionados, identifica competencias bajas (≤2) y altas (≥4), y genera una frase sintética sin llamar a Gemini. Se llama desde `window._calCompSetNivel` solo si `txt.dataset.manualEdit !== '1'` (no sobrescribe ediciones manuales del usuario). El textarea tiene `oninput="this.dataset.manualEdit='1'"` para bloquearlo una vez tocado manualmente.
- `2026-06-21` — feat(calificaciones): **sección competencial LOMLOE en boletín PDF** (`js/calificaciones.js`). `_calBoletinPDF` ahora consulta `comentarios_competenciales` por `alumno_nombre` y añade, al final del PDF, una tabla 4 columnas (Asignatura / Evaluación / Competencias con nivel / Observación). Silencia el error si la tabla aún no existe. `_NIV_LBL = ['','Iniciado','En proceso','Adquirido','Avanzado']`.
- `2026-06-21` — feat(calificaciones): **pills competenciales en la ficha de alumno (vista admin)** (`js/calificaciones.js`). `_calLoadCompetencialAdmin(panel, nombre)` — función async llamada desde `_calAbrirAlumno`; carga `comentarios_competenciales` en segundo plano y añade al panel derecho pills coloreadas por nivel (danger=Iniciado, warning=En proceso, info=Adquirido, success=Avanzado) agrupadas por asignatura. Doble guarda stale: comprueba `_calSelectedAlumno === nombre` antes de insertar HTML. Silencia el error si la tabla no existe.

- `2026-06-20` — feat(landing): **landing page actualizada — de 8 a 25+ módulos** (`index.html`). Titular "Ocho módulos" → "Más de 25 módulos. Un solo acceso." + subtítulo más concreto. Chips orbitales actualizados (Admin→"Agentes IA", App PWA→"Familias"). Lista de módulos expandida de 8 a 12 ítems interactivos (añadidos 09 Planner, 10 Calificaciones, 11 Salidas, 12 Agentes IA) + teaser grisado "Y 13 módulos más" (Agenda, Tutorías, Orientación, Encuestas, Documentos, Mensajería, Analytics, Recursos, Espacios, Calidad, IB, Asistencia, Alumnos). 4 pantallas mock nuevas (`screen-8` tablero Planner con colores por materia, `screen-9` gradebook con comentarios IA, `screen-10` gestión de salidas con KPIs, `screen-11` panel de 3 agentes IA con alertas en vivo). `moduloData` JS actualizado con los 12 módulos y sus features reales. Commit `eb64b2f`.
- `2026-06-20` — fix(migraciones): **aplicadas 4 migraciones del portátil pendientes en producción** (`scripts/ejecutar-migraciones-laptop.mjs` nuevo). Las migraciones creadas en sesión del portátil nunca se habían aplicado en Supabase → módulo Mensajería roto en prod (tabla 42P01), toggle 3 estados comedor sin efecto, traducción multi-idioma comunicados silenciosa. Aplicadas vía Management API (`sbp_xxx`): (1) `asistencia_comedor.tupper bool DEFAULT false` — toggle No/Tupper/Menú; (2) `profiles.idioma text DEFAULT 'es'` — idioma preferido comunicados; (3) tabla `mensajes` + RLS `msg_familia`/`msg_staff` — módulo Mensajería ahora funcional en prod; (4) tabla `personas_autorizadas` + RLS `pa_familia`/`pa_staff` — personas autorizadas a recoger al alumno. Script `scripts/ejecutar-migraciones-laptop.mjs` creado para futuras necesidades (intenta Management API, fallback a service role). `scripts/schema-snapshot.json` regenerado: 58 → 72 tablas, 0 column mismatches. CLAUDE-TABLAS.md completado: 4 filas de módulos por rol faltantes añadidas (Tutorías, Mensajería, Agentes IA, Pasar lista). Commit `738cd6b`.
- `2026-06-19` — feat(agente): **versión PROGRAMADA del agente de sustituciones** (`supabase/functions/agente-cobertura-diaria/` + `supabase/migrations/20260619_cron_agente_cobertura.sql`). Modo "preparar + avisar": pg_cron `agente-cobertura-diaria` (`0 6 * * 1-5` ≈ 8:00 España, salta finde) → Edge Function que detecta las ausencias del día, expande las de día completo a tramos, y propone un sustituto por tramo (guardia → equidad, asignación global sin conflictos — el mismo cerebro del agente cliente portado a Deno: `norm`/`esGuardia`/`coincide`, candidatos por hora, allocation escasez-primero). **Envía el plan por email** (Resend, tabla HTML + botón a la app) **+ push** a dirección; **NO asigna nada** (la dirección confirma en la app). Solo envía si hay tramos sin cubrir; hoy en `Europe/Madrid`. Validado end-to-end en Buñol (propone profe de guardia por equidad, email enviado). Programado **solo para Buñol** (centro de pruebas); extensible a todos los centros cambiando el body del cron a `{}`. **Cierra la línea de agentes de esta sesión: 4 agentes asistidos + panel central + 1ª versión programada.**
- `2026-06-19` — feat(agentes): **panel central de Agentes** (`js/agentes.js` nuevo + `app.html` + `js/auth.js`). Pestaña "🤖 Agentes" (sidebar bajo Asistente IA, color terracota; visible admin/admin_institucional/director/jefatura/superadmin) → `initAgentesPanel()`: lanzador con una tarjeta por agente (sustituciones/RRHH/comunicación/orientación), cada una con descripción, **dato en vivo** (tramos sin cubrir hoy / solicitudes pendientes / alertas activas, consultado por tarjeta) y botón "Lanzar agente" (`_agtLanzar`→`window[fn]()`). Los agentes abren sus modales sobre cualquier panel. `_rrhhEnsureData()` en `agenteRRHH` para cargar las ausencias al lanzarse desde fuera de RRHH. Cableado tab/nav/panel/TITLES/MAS_CFG/TAB_MAP/showTab. SW v18→v19.
- `2026-06-19` — feat(agente): **agente de orientación/riesgo (modo asistido)** (`js/orientacion.js`). Cuarto agente. Botón "🤖 Agente de riesgo" en el panel de riesgo → `agenteOrientacion()`: cruce determinista (faltas de aula + notas + incidencias 30d, `_oriCalcularRiesgo` reutilizable) → alumnado en riesgo con señales/nivel/tipo; para los de mayor riesgo (tope `_AGORI_MAX_IA=12`) redacta con IA (`_oriGemini`) un borrador de alerta (2-4 frases, prudente) con barra de progreso, el resto lleva el resumen de señales. Bandeja asistida: cada alumno con badge + señales + borrador editable + checkbox → "Crear alertas seleccionadas" inserta en `alertas_orientacion` (`descripcion_ia`=borrador editado) y avisa a orientación/dirección (`_oriNotificarAlerta`). SW v17→v18. **Con esto, los 4 agentes de la primera tanda están construidos** (sustituciones, RRHH, comunicación, orientación), todos en modo asistido.
- `2026-06-19` — feat(agente): **agente de comunicación a familias (modo asistido)** (`js/comunicados.js`). Tercer agente. Botón "🤖 Agente: redactar" en Comunicados → `agenteComunicacion()`: a partir de una orden breve, Gemini (`_comIA`, role:familia) redacta el comunicado (título + cuerpo cordial/profesional) e **infiere los destinatarios** (todas las familias / `grupo:XXXX` / todo el centro / profesorado) → JSON parseado (`_agcParse`). Borrador editable (asunto, mensaje, destinatarios + grupo) → "✓ Revisar y enviar" → núcleo `_comEnviarNucleo` (insert en `comunicados` + email `send-comunicado` + push `_comPushFamilias`). Cada familia lo recibe en su idioma al abrirlo (multi-idioma `_comTraducir` ya existente). Modo asistido. SW v16→v17.
- `2026-06-19` — feat(agente): **agente de RRHH/permisos (modo asistido)** (`js/rrhh.js`). Segundo agente. Botón "🤖 Agente: evaluar pendientes" en RRHH → `agenteRRHH()`: evalúa TODAS las solicitudes pendientes contra la normativa del centro (concertada/privada/EBEP, según `_rrhhGetRegimen`) en secuencia con barra de progreso (`_rrhhEvaluarUna` → `_rrhhGeminiLegal`), y monta una bandeja con, por solicitud: recomendación (aprobar/denegar/revisar con color), explicación + fundamento orientativo, alerta, y un mensaje al profesor ya redactado y editable; botones "Aprobar/Rechazar y enviar" por solicitud + "Aprobar las N recomendadas" (bulk de las claras). Refactor: `_rrhhResolver` extrae `_rrhhAplicar(id,accion,mensaje)` (núcleo sin UI: estado + nota_resolucion + sustituciones + push al profesor), reutilizado por el modal individual y por el agente. SW v15→v16.
- `2026-06-19` — feat(agente): **agente de sustituciones autónomo (modo asistido)** (`js/agente.js` nuevo + refactor `js/admin.js`). Primer agente de DidactIA. Botón "🤖 Agente: cubrir el día" en Sustituciones → `agenteSustituciones(fecha)`: expande ausencias de día completo a tramos y produce un plan de cobertura con **asignación global**: prioriza profesorado **de guardia** por tramo, luego libres por **equidad**; **nunca asigna el mismo profesor a dos clases a la vez** (`usedByHora`); reparte la carga del día (`load` proyectado) y resuelve antes los tramos con menos candidatos (escasez primero). **Briefing** en lenguaje natural + tabla con cada propuesta como desplegable editable (guardia 🛡/libre con su carga) + avisos de tramos sin cobertura. **Modo asistido**: propone, tú confirmas → "Confirmar y asignar" ejecuta todo (UPDATE + guardia + email a ambos vía notify-sustitucion + push al sustituto). Refactor de `_sustCalcularLibres` para exponer `guardia` y `libres` por separado (compat conservada para el formulario y "+ Asignar"). El botón "Plan manual" (`planCoberturaDia`) se conserva como alternativa. SW v14→v15.
